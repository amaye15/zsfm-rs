[project]
name = "timesfm-v2-5-env"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["timesfm[torch]"]

[tool.uv.sources]
timesfm = { git = "https://github.com/google-research/timesfm.git" }

[tool.uv]
package = false
