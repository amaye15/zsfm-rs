[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "ttm-rs"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
module-name = "ttm_rs"

[dependency-groups]
dev = [
    "maturin>=1.14.0",
]
